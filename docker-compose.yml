name: unifyhub-frontend

services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      HOSTNAME: 0.0.0.0
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
